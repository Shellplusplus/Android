<template>
  <div class="page">
    <img src="/common/About_336.png" class="about-bg about-336" />
    <img src="/common/About_192.png" class="about-bg about-192" />
    <img src="/common/About_212.png" class="about-bg about-212" />
    <img src="/common/back.png" @click="goBack" class="hd-back" />
    <div class="pill-header">
      <div class="pill-more-wrap" @click="goBack">
        <div class="pill-more"></div>
      </div>
    </div>
  </div>
</template>

<script>
import router from "@system.router"

export default {
  goBack() {
    router.back()
  }
}
</script>

<style>
.page { width: 336px; height: 480px; background-color: #000000; position: relative; }
.about-bg { position: absolute; left: 0; top: 0; }
.about-336 { width: 336px; height: 480px; }
.about-192 { display: none; width: 192px; height: 490px; }
.about-212 { display: none; width: 212px; height: 520px; }
.hd-back { position: absolute; left: 6px; top: 6px; width: 72px; height: 72px; }
.pill-header { display: none; }
@media (shape: pill-shaped) and (max-width: 100) {
  .page { width: 192px; height: 490px; }
  .about-336 { display: none; }
  .about-192 { display: flex; }
  .about-212 { display: none; }
  .hd-back { display: none; }
  .pill-header { display: flex; width: 192px; height: 92px; flex-direction: column; align-items: center; justify-content: flex-start; position: absolute; top: 0; left: 0; }
  .pill-more-wrap { width: 92px; height: 72px; border-radius: 32px; flex-direction: row; justify-content: center; align-items: center; margin-top: 10px; }
  .pill-more { width: 92px; height: 72px; background-image: url(/common/back_capsule.png); background-size: 92px 72px; border-radius: 32px; }
}
@media (shape: pill-shaped) and (min-width: 101) {
  .page { width: 212px; height: 520px; }
  .about-336 { display: none; }
  .about-192 { display: none; }
  .about-212 { display: flex; }
  .hd-back { display: none; }
  .pill-header { display: flex; width: 212px; height: 92px; flex-direction: column; align-items: center; justify-content: flex-start; position: absolute; top: 0; left: 0; }
  .pill-more-wrap { width: 102px; height: 72px; border-radius: 36px; flex-direction: row; justify-content: center; align-items: center; margin-top: 10px; }
  .pill-more { width: 102px; height: 72px; background-image: url(/common/back_capsule.png); background-size: 102px 72px; border-radius: 36px; }
}
</style>
