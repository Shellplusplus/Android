# 安全策略

Shell++ 可以向手表发送远程命令、传输文件并接收局域网上传，因此安全问题可能直接影响设备和用户数据。

## 报告漏洞

请优先通过 GitHub 仓库的 **Security → Report a vulnerability** 私下提交报告。不要在公开 Issue、Pull Request、群聊或日志截图中披露可复现的利用细节。

如果仓库暂未启用私有漏洞报告，请创建一个不包含敏感细节的 Issue，请求维护者提供私密联系方式。

报告中建议包含：

- 受影响的版本或源码引用
- 受影响模块和前置条件
- 最小复现步骤或概念验证
- 可能影响的数据、设备或权限
- 建议修复方向（如有）

请勿在测试中访问不属于你的设备、账号或网络，也不要公开真实 token、签名材料、设备标识、命令输出或诊断报告。

## 高风险边界

以下问题请按高优先级私下报告：

- 绕过远程终端命令校验或请求来源校验
- 未授权读取、写入或删除手表端文件
- 局域网 HTTP 服务的路径穿越、任意文件写入或拒绝服务
- 更新包来源、完整性或安装授权绕过
- AI 授权签名、设备身份或撤销检查绕过
- 诊断导出或日志中的密钥、标识符和用户数据泄露

## 支持范围

维护者优先处理当前默认分支和最新公开版本。较旧版本可能只提供升级建议，不保证单独回补安全修复。
